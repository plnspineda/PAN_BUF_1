# Computation of graph complexity

1. To get the 100Kb window, use `scripts/run_odgi_stat_region_noslide.sh`.
2. Add color and depth to the gfa file using `plot_scripts/add_depth_gfa.py` and `plot_scripts/add_depth_gfa.py`